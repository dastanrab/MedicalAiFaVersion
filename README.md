
  # Medical AI Mobile App UI

  This is a code bundle for Medical AI Mobile App UI. The original project is available at https://www.figma.com/design/5f7ftfbWU8O0MeXk0LZlSM/Medical-AI-Mobile-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  